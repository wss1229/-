1、强缓存：响应头（Expires）上，Expires表示资源过期时间，如果客户端在过期之前再次获取资源就可以直接在缓存里面拿，不再访问服务器
          优点：减少服务器读取磁盘压力
          缺点：缓存过期后，不管文件没有变化都会再次请求服务器；缓存过期时间依赖于客户端时间，客户端时间会被改动
          请求/响应头（Cache-Control），
2、协商缓存：If-Modified-Since（请求头，资源最近修改时间，由浏览器告诉服务器）和Last-Modified（响应头，资源最近修改时间）
             Etag（响应头，资源标识）和If-None-Match（资源标识）
             相同点：这两个都是通过客户端第一次请求服务器返回标识来确认是否需要从缓存读取
             不同点：Last-Modified是由精确到秒的时间组成而Etag由文件的大小和时间组成，后者更精确

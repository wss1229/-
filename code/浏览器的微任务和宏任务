1.1 首先js是一个单线程的脚本语言，但是有异步事件的概念。
1.2 微任务和宏任务的概念：
      任务队列中的都是已经完成的异步操作，而不是说注册一个异步任务就会被放在这个任务队列中，也就是说微任务和宏任务都是异步操作，都需要在同步代码执行完后执行
      在当前微任务没有执行完成时，是不会执行下一个宏任务的
      所有会进入的异步都是指的事件回调中的那部分代码
      执行顺序：在同步代码执行完成后，检查是否有异步任务完成并执行对应的回调，并且微任务会在宏任务之前执行
      那么判断哪些宏任务和微任务就显得很重要了：
                      宏任务：setTimeoout,setInterval,requestAnimationFrame
                      微任务：MutationObserver,Promise.then catch finally,await
